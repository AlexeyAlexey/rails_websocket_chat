<% if user_signed_in? %>
  <span>Hello, <%= current_user.email %></span>
  <p><%=link_to "Chatroom", chatrooms_path%></p>
  <p><%=link_to 'Exit', destroy_user_session_path, method: :delete %></p>
<% else %>
  <%=link_to 'Enter', new_user_session_path %> or <%= link_to 'Registration', new_user_registration_path %>
<% end %>